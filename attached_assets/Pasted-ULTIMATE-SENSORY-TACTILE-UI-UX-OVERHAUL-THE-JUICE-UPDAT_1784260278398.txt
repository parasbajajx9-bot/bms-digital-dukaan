ULTIMATE SENSORY & TACTILE UI/UX OVERHAUL (THE "JUICE" UPDATE)

Please apply advanced, premium UI/UX micro-interactions, spring animations, and tactile feedback mechanisms across the entire application. We want the app to feel incredibly satisfying, premium, and addictive to use.

---

### STEP 1: SPRING-LOADED TACTILE BUTTONS & HOVERS
- Apply a physical, spring-loaded tactile animation to ALL primary action buttons (e.g., "Finalize Bill", "Add Product", "Log Udhaar").
  - On Hover: Subtle lift up 'hover:-translate-y-[2px]', soft background color shifts, and an elegant ambient colored glow drop-shadow.
  - On Active (Click): Physical compression scale 'active:scale-95 duration-100 transition-all ease-out'.
- Card Hovers: Make key dashboard metric cards feel interactive. On hover, apply a super smooth zoom scale-101 and light border tint transition.

---

### STEP 2: DELIGHTFUL CELEBRATIONS & "JUICY" FEEDBACK LOOPS
- Add a canvas-confetti transition or a beautiful micro-animation to key milestone completions:
  - When "Finalize Bill" is clicked and successfully processed, trigger a soft, premium micro-burst of multi-colored confetti from the button position.
  - When an Udhaar entry is marked as "Fully Paid", trigger a beautiful, fluid green checkmark scale animation with a soft background pulse.
- This creates instant positive reinforcement, rewarding the user for completing data tasks.

---

### STEP 3: SMOOTH DASHBOARD "COUNT-UP" NUMERIC ANIMATIONS
- For all major analytical display numbers on the dashboard (Total Revenue, Margin %, Break-Even Point, and Total Outstanding Udhaar), do not let them flatly snap onto the screen.
- Implement a smooth, dynamic "Count-Up" transition on page load (e.g., smoothly counting up from 0 to the target number over 1.2 seconds with an ease-out timing curve).
- Keep the numbers rolling in real-time when filters or date ranges change to make the data feel alive.

---

### STEP 4: AMBIENT GLOWS, FROSTED GLASS & MOTION TRANSITIONS
- MODAL TRANSITIONS: Replace flat modal appearances. Modals must smoothly slide/scale up with dynamic cubic-bezier curves ('cubic-bezier(0.34, 1.56, 0.64, 1)') from a scale of 0.95 to 1.0, paired with an elegant 'bg-black/30 backdrop-blur-md' overlay.
- PROGRESS BARS: Add a shimmering glow reflection sweep animation (using an absolute positioned Tailwind linear gradient sweep) that passes over progress bars and active state badges every few seconds.
- EMPTY STATES: If there are no products, bills, or outstanding debts, design a gorgeous, minimalist custom illustration or animated icon saying "All clean! Let's start a new record" with a pulsating, friendly CTA button.